# Enforce LF line endings for all text files so the repo is consistent
# across Windows, macOS, and Linux (and matches what Prettier writes).
* text=auto eol=lf

# Binary files — never convert
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
